/// <reference types="react" />
import { TextProps } from '../types';
export default function createText({ size, color, center, bold, italic, oblique, smallCaps, invisible, transform, numberOfLines, style, ...rest }: TextProps | any, ref: any, map: any): JSX.Element;
