/// <reference types="react" />
import { ButtonProps } from '../types';
export default function createButton({ variant, size, block, loading, style, children, ...rest }: ButtonProps | any, ref: any, map: any): JSX.Element;
