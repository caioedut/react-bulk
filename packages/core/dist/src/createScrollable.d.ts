/// <reference types="react" />
import { ScrollableProps } from '../types';
export default function createScrollable({ horizontal, style, ...props }: ScrollableProps | any, ref: any, map: any): JSX.Element;
