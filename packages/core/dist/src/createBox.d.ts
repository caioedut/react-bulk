/// <reference types="react" />
import { BoxProps } from '../types';
export default function createBox({ component, className, flexbox, direction, wrap, flow, justifyContent, alignContent, justifyItems, alignItems, center, gap, flex, order, grow, shrink, basis, align, justify, style, children, ...props }: BoxProps | any, ref: any, map: any, defaultComponent?: any): JSX.Element;
