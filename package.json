{
  "name": "root",
  "private": true,
  "author": "CaioEduT <caioedut@gmail.com>",
  "homepage": "https://github.com/caioedut/react-bulk",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "example:web": "yarn build && yarn --cwd ./examples/web dev",
    "example:native": "yarn build && yarn --cwd ./examples/native dev",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna exec --parallel -- rimraf dist typings",
    "prebuild": "npm run clean",
    "build": "lerna exec --parallel -- tsc --build",
    "publish:stable": "npm run build && lerna publish"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^3.2.0",
    "@types/node": "^18.7.6",
    "@types/react": "^17.0.2",
    "@types/react-native": "^0.63.4",
    "bili": "^5.0.5",
    "lerna": "^5.1.4",
    "prettier": "^2.6.2",
    "react": "^17.0.2",
    "react-native": "^0.63.4",
    "rimraf": "^3.0.0",
    "rollup-plugin-typescript2": "^0.32.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {}
}
